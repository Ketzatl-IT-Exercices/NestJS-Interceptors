import { LogsClientsInterceptor } from './logs-clients.interceptor';

describe('LogsClientsInterceptor', () => {
  it('should be defined', () => {
    expect(new LogsClientsInterceptor()).toBeDefined();
  });
});
